https://prismjs.com/#examples

http://vizsga.danielcsohany.testhosting.hu/

http://www.informatikaismeretek.nhely.hu/

for code before saving to json :

replace every line break with \n
replace every 4 space with 2
replace every 4 space with \t
done



function throttle(func, limit) {
    let lastFunc;
    let lastRan;
    return function (...args) {
        const context = this;
        if (!lastRan) {
            func.apply(context, args);
            lastRan = Date.now();
        } else {
            clearTimeout(lastFunc);
            lastFunc = setTimeout(function () {
                if (Date.now() - lastRan >= limit) {
                    func.apply(context, args);
                    lastRan = Date.now();
                }
            }, limit - (Date.now() - lastRan));
        }
    };
}



    function AtBeforeUnload() {
        // save all stuff before leaving
        console.log("yeet");
        CartLocalSet();
    }

    // Attach the function to the beforeunload event
    window.addEventListener("beforeunload", AtBeforeUnload);